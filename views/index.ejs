<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Birthday App</title>
  <style>
    body { font-family: sans-serif; text-align: center; margin-top: 50px; }
    .login-btn { display: inline-block; padding: 10px 20px; background-color: #00C300; color: white; text-decoration: none; border-radius: 5px; }
    .logout-btn { display: inline-block; margin-top: 20px; color: gray; }
    h1 { color: #E83E8C; }
  </style>
</head>
<body>

  <% if (user) { %>
    <% if (message) { %>
      <h1><%= user.displayName %>さん、<br><%= message %></h1>
    <% } %>
    
    <a href="/logout" class="logout-btn">ログアウト</a>

  <% } else { %>
    <h2>LINEでログインしてください</h2>
    <a href="/login" class="login-btn">LINEログイン</a>
  <% } %>

</body>
</html>
