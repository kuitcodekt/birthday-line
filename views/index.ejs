<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>.......</title>
    <style>
        body { font-family: sans-serif; text-align: center; margin-top: 50px; }
        .login-btn { background-color: #00B900; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; }
        .message { font-size: 2em; color: #ff4081; }
        .user-info { margin-top: 20px; }
    </style>
</head>
<body>
    <% } else { %>
        <p>LINEでログインしてお祝いメッセージをチェック！</p>
        <a href="/login" class="login-btn">LINEでログイン</a>
    <% } %>

</body>
</html>
